sudo: required
language: node_js
node_js:
  - "0.10"
before_install:
  - "curl https://install.meteor.com | /bin/sh"

script: SELENIUM_BROWSER=phantomjs meteor --test --release velocity:METEOR@1.1.0.3_2